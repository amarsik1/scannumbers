const meterData = require('./meterData');
const consumer = require('./consumer');
const meter = require('./meter');
const address = require('./address');
const metersGroup = require('./metersGroup');
const organization = require('./organization');

module.exports = {
    meterData,
    consumer,
    meter,
    address,
    metersGroup,
    organization
};